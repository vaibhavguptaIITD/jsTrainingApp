<div class="panel panel-default instructions-panel">
	<div class="panel-heading">
		<h3 class="panel-title">Object oriented javascript</h3>
	</div>
	<div class="panel-body">
		<h5>
			Constructor
		</h5>
		<p>Two ways to create an object</p>
		<ul>
			<li>
				<b>Literal</b>: using <code>{}</code>
			</li>
			<li>
				<b>Constructor</b>: using <code>new</code> on <code>function</code>
			</li>
		</ul>
		<h5>Syntax</h5>
<pre>
function Person(arg1, arg2){
	//some code here
}
var me = new Person(arg1, arg2);
</pre>
In the above code <code>Person</code> is the <b>class</b> and the function is <b>constructor</b>.
	</div>
	<div class="bg-info exercisePanel">
		<h4>Exercise</h4>
		<ul>
			<li>
				<p>Create a constructor for <code>Person</code></p>
			</li>
			<li>
				<p>Create a new object of <code>Person</code> called <code>me</code></p>
			</li>
			<li>
				log variable <code>me</code> and observe the output.
			</li>
		</ul>
	</div>
</div>