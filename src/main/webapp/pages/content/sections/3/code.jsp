//Make the following evaluate to statements true. 
//Replace "?" with appropriate operator

log(1 ? 2);

log("efgh".length ? "abc".length);

log(1 ? 1);

log("1" ? 1); 

//Now make the last statement false

log("1" ? 1);

